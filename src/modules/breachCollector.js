'use strict';

// Backwards-compatible name for the only supported breach provider. The old
// implementation generated random breach records and was intentionally removed.
module.exports = require('./hibpCollector');
